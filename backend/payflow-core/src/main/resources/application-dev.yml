spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payflow
    username: payflow
    password: payflow
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    com.payflow: DEBUG
