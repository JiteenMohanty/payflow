apiVersion: 1

providers:
  - name: payflow
    orgId: 1
    folder: PayFlow
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
